# Paste into hopsakee-server/config/caddy/conf/Caddyfile.
#
# PUBLIC app — no `import authelia_forward_auth` / `import logout`, matching
# the pkw-web / hopsakee-decimal-finder precedent (MIGRATION-PLAN.md locks
# every Tier-A static app as public; access control is "nothing sensitive
# here", not auth). A gated app would additionally import both and add the
# `handle_errors 403 503` block — see hopswiki.hopsakee.top in the same file
# for that shape.

ren-afstand.hopsakee.top {
	reverse_proxy ren-afstand:8080
}
